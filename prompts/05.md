
Você é um arquiteto de software sênior especialista em criptografia aplicada, canonicalization, secret management, envelope encryption, event sourcing, documentação técnica e especificações auditáveis.

Sua tarefa é aplicar uma revisão final de consistência no README atual do projeto:

# Linus Salamander Enclave System (LSES)

O README já está tecnicamente forte e deve preservar sua estrutura atual. Não reescreva tudo do zero. Faça apenas ajustes editoriais e técnicos de consistência para alinhar todos os termos com o modelo final de identidade criptográfica.

Contexto atual da especificação:

LSES usa:
- Purge-on-Write
- GitHub como storage não confiável
- envelope encryption
- AES-256-GCM
- DEK por snapshot
- wrapped_dek
- KEK/KMS/TPM/Nitro/recipient public key para wrapping
- Ed25519 para assinatura do Enclave
- Consumer countersignature
- epoch
- previous_snapshot_hash
- trusted_chain_head
- RuntimeSecretHandle por padrão
- persistence_receipt separado da identidade criptográfica
- snapshot_body como conteúdo criptográfico canônico
- snapshot_id como hash de canonical_snapshot_body
- attestations assinando canonical({ snapshot_id, snapshot_body })

A estrutura correta do modelo é:

snapshot_body
→ canonical_snapshot_body
→ snapshot_id
→ signature_input = canonical({ snapshot_id, snapshot_body })
→ attestations
→ persistence_receipt

O objetivo desta revisão é garantir que o README inteiro use essa modelagem de forma consistente.

Aplique as seguintes correções obrigatórias:

## 1. Remover duplicação em Testing Strategy

Na seção “Testing Strategy”, há duas linhas consecutivas com:

### Unit Tests
### Unit Tests

Remova a duplicação e mantenha apenas uma.

## 2. Atualizar termos antigos nos Security Invariants

Na seção “Security Invariants”, revise qualquer uso ambíguo de:

- canonical envelope
- canonicalized envelope
- envelope body

E substitua, quando fizer sentido, pelos termos finais:

- canonical_snapshot_body
- snapshot_body
- signature_input = canonical({ snapshot_id, snapshot_body })

Exemplo:

Antes:
“All signed data must be canonicalized before signing.”

Depois:
“All signed data must be represented as `signature_input = canonical({ snapshot_id, snapshot_body })` before Enclave signature and Consumer countersignature.”

Antes:
“A snapshot is invalid unless both signatures verify against the canonical envelope.”

Depois:
“A snapshot is invalid unless both signatures verify against the same `signature_input = canonical({ snapshot_id, snapshot_body })`.”

Não altere o sentido dos invariants. Apenas alinhe a terminologia.

## 3. Atualizar GitHub Persistence Contract

Na seção “GitHub Persistence Contract”, adicione explicitamente que GitHub pode armazenar a estrutura final:

```text
{
  snapshot_body,
  snapshot_id,
  attestations,
  persistence_receipt
}
```

Explique:

* `snapshot_body` contém o conteúdo criptográfico selado.
* `snapshot_id` identifica deterministicamente o snapshot.
* `attestations` provam aceite criptográfico do Enclave e do Consumer.
* `persistence_receipt` registra onde o snapshot foi armazenado.
* GitHub continua sendo não confiável.
* A verificação criptográfica do snapshot não depende do GitHub ou do `commit_sha`.

Preserve a lista “GitHub must never store”.

## 4. Corrigir Read Secret Flow

Na seção “Read Secret Flow”, adicione explicitamente a recomputação do `snapshot_id` antes da validação das assinaturas.

O fluxo deve ficar logicamente assim:

1. Receive `SecretReadRequest`.
2. Validate consumer identity and access policy.
3. Fetch the latest snapshot structure from GitHub.
4. Canonicalize `snapshot_body`.
5. Recompute `snapshot_id = sha256(canonical_snapshot_body)`.
6. Compare recomputed `snapshot_id` with stored `snapshot_id`.
7. Compute `signature_input = canonical({ snapshot_id, snapshot_body })`.
8. Verify Enclave signature and Consumer countersignature over the same `signature_input`.
9. Validate `previous_snapshot_hash` against the local chain head.
10. Validate `epoch > last_accepted_epoch`.
11. Unwrap `wrapped_dek` strictly within the Enclave boundary.
12. Verify AES-GCM auth tag and unseal the payload into `EnclaveMemoryStore`.
13. Return `RuntimeSecretHandle` by default.
14. Purge plaintext DEK and sensitive material after the handle expires or is consumed.

Deixe claro que:

* hash/signature validation happens before DEK unwrap whenever possible.
* DEK unwrap and AES-GCM decrypt happen only after structural, hash, signature, chain and policy checks pass.
* plaintext export requires explicit policy authorization.

## 5. Atualizar Recovery Flow

Na seção “Recovery Flow”, adicione a mesma lógica de verificação:

* canonicalize `snapshot_body`
* recompute `snapshot_id`
* verify attestations over `signature_input`
* validate chain continuity
* validate trust anchor
* only then unwrap DEK/decrypt if needed

Explique que recovery deve validar `{ snapshot_body, snapshot_id, attestations }` sem depender de `commit_sha`.

## 6. Adicionar termos em Terminology

Na seção “Terminology”, adicione os seguintes termos:

### Snapshot Body

The finalized cryptographic content of a snapshot. It contains metadata, AAD, nonce, sealed payload, auth tag, wrapped DEK, key identifiers, epoch and previous snapshot hash.

### Snapshot ID

A deterministic hash computed as `sha256(canonical_snapshot_body)`. It must not include itself, attestations, or persistence receipts.

### Attestations

The set of signatures proving that the Enclave and Consumer accepted the same finalized snapshot identity and body.

### Signature Input

The canonical byte sequence signed by both parties:
`signature_input = canonical({ snapshot_id, snapshot_body })`.

### Persistence Receipt

Provider-specific storage metadata, such as GitHub repository, branch, commit SHA and committed timestamp. It does not define the cryptographic identity of the snapshot.

## 7. Atualizar Testing Strategy

Além de remover a duplicação de `### Unit Tests`, adicione ou garanta estes testes:

### Unit Tests

* Read flow recomputes `snapshot_id` before signature verification.
* Signature verification uses `signature_input = canonical({ snapshot_id, snapshot_body })`.
* `commit_sha` is never required to verify cryptographic identity.
* Terminology is consistent: no implementation test refers to `commit_sha` as snapshot identity.

### Integration Tests

* Read flow rejects a snapshot when stored `snapshot_id` differs from recomputed `snapshot_id`.
* Recovery verifies `{ snapshot_body, snapshot_id, attestations }` without depending on `persistence_receipt`.
* GitHub adapter stores `{ snapshot_body, snapshot_id, attestations, persistence_receipt }`.

### Property-Based Tests

* Any mutation to `snapshot_body` changes recomputed `snapshot_id`.
* Any mutation to `attestations` does not change `snapshot_id` but causes signature verification failure.
* Any mutation to `persistence_receipt` does not change `snapshot_id`.

## 8. Ajustar linguagem final

Mantenha tom técnico, preciso e auditável.

Evite:

* impossible
* immune
* guaranteed
* unbreakable
* fully secure

Prefira:

* rejects
* detects
* mitigates
* requires
* under the stated threat model
* best-effort

Resultado esperado:

Produza uma versão atualizada do README em Markdown.

Preserve o nome:

# Linus Salamander Enclave System (LSES)

Não remova as seções existentes.

O resultado final deve deixar claro que:

1. `snapshot_body` é o conteúdo criptográfico canônico.
2. `snapshot_id = sha256(canonical_snapshot_body)`.
3. `snapshot_id` não inclui ele mesmo.
4. `attestations` assinam `signature_input = canonical({ snapshot_id, snapshot_body })`.
5. `persistence_receipt` é externo à identidade criptográfica.
6. `commit_sha` não participa da verificação criptográfica do snapshot.
7. Read e Recovery recomputam `snapshot_id` antes de validar assinaturas.
8. GitHub armazena envelopes e recibos, mas não é fonte confiável de validade.
9. A terminologia do README inteiro fica consistente com esse modelo.

```

Esse prompt é para a limpeza final do README atual, que já contém o modelo com `snapshot_body`, `snapshot_id`, `attestations` e `persistence_receipt`, além de invariants e flows compatíveis com a separação entre identidade criptográfica e recibo de persistência. :contentReference[oaicite:0]{index=0}
```
